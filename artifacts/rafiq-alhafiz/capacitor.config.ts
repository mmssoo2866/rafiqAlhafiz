import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rafiqalhafiz.app',
  appName: 'Rafiq AlHafiz',
  webDir: 'dist/public',
  bundledWebRuntime: false
};

export default config;
